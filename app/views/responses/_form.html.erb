<%= form_with(model: response, local: true) do |form| %>
  <% if response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(response.errors.count, "error") %> prohibited this response from being saved:</h2>

      <ul>
      <% response.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :forget %>
    <%= form.check_box :forget, id: :response_forget %>
  </div>

  <div class="field">
    <%= form.label :medical %>
    <%= form.check_box :medical, id: :response_medical %>
  </div>

  <div class="field">
    <%= form.label :allowance %>
    <%= form.check_box :allowance, id: :response_allowance %>
  </div>

  <div class="field">
    <%= form.label :other %>
    <%= form.check_box :other, id: :response_other %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
